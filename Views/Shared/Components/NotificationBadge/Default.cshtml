@model int

@if (Model > 0)
{
    <span class="badge rounded-pill bg-danger position-absolute translate-middle" id="notificationDigit"style="font-size: 0.65rem;">
        @(Model > 99 ? "99+" : Model.ToString())
    </span>
}